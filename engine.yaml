# The engine yaml api version
apiVersion: "3.0.0"

# The identifier for this repository
repository: mikeldking/examples

# Set environment that will be available to your code
environment:
  why: this

# The command to run
command: python tensorflow/mnist/tensorflow_mnist.py --data-dir ./data/mnist --batch-size 32
